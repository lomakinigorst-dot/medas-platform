# Включить playwright-mcp

Запусти в терминале:
```bash
claude mcp add playwright -- npx -y @playwright/mcp@latest
```

Затем **перезапусти VS Code** — playwright будет доступен в новой сессии.

⚠️ После выполнения задачи с браузером — запусти `/playwright-off`.
