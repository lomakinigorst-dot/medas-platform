# CODE_MAP — Карта кода [PROJECT_NAME]
> Обновлён: [YYYY-MM-DD] | Читать вместо поиска по коду каждую сессию

---

## БЫСТРЫЙ ПОИСК (TOC — читать вместо grep)

| Раздел | Строка |
|---|---|
| СТРУКТУРА ПРОЕКТА (дерево папок) | 20 |
| СТРАНИЦЫ → КОМПОНЕНТЫ (URL-маппинг) | 32 |
| API ENDPOINTS | 43 |
| БД МОДЕЛИ | 58 |
| КЛЮЧЕВЫЕ КОМПОНЕНТЫ / СЕРВИСЫ | 70 |
| КОНФИГУРАЦИЯ | 80 |
| ДИЗАЙН-СИСТЕМА | 90 |
| ДЕПЛОЙ — команды | 105 |
| ВАЖНЫЕ ПАТТЕРНЫ | 117 |
| ФАЙЛЫ НЕ ТРОГАТЬ | 130 |

**ПРАВИЛО:** При добавлении нового раздела → обновить эту таблицу с правильным номером строки.

---

## СТРУКТУРА ПРОЕКТА

```
[project-name]/
├── [папка]/        # [что тут]
├── [папка]/        # [что тут]
└── [папка]/        # [что тут]
```

---

## FRONTEND — СТРАНИЦЫ → КОМПОНЕНТЫ

| URL | Файл страницы | Компонент |
|---|---|---|
| `/` | `app/page.tsx` | `components/[MainPage].tsx` |
| `/[route]` | `app/[route]/page.tsx` | `components/[Component].tsx` |
| `[добавляй по ходу]` | | |

---

## BACKEND — API ENDPOINTS

### [Группа 1 — например: Auth] `/api/v1/auth`
| Метод | Путь | Файл | Строка |
|---|---|---|---|
| POST | `/login` | `endpoints/auth.py` | [N] |
| `[добавляй]` | | | |

### [Группа 2 — например: Users] `/api/v1/users`
| Метод | Путь | Файл | Строка |
|---|---|---|---|
| GET | `/` | `endpoints/users.py` | [N] |
| `[добавляй]` | | | |

---

## БД — КЛЮЧЕВЫЕ МОДЕЛИ

### [ModelName] (`models/[file].py:[строка]`)
```
id (UUID PK)
[поле]  [тип]
[поле]  [тип]
[добавляй поля по ходу]
```

---

## КЛЮЧЕВЫЕ КОМПОНЕНТЫ / СЕРВИСЫ

| Компонент/Сервис | Путь | Роль |
|---|---|---|
| `[ComponentName]` | `[путь]` | [что делает] |
| `[добавляй]` | | |

---

## КОНФИГУРАЦИЯ

| Что | Где задаётся | Дефолт |
|---|---|---|
| API Base URL | `.env → API_BASE_URL` | `http://localhost:8000` |
| Порт frontend | `.env → PORT` | `3000` |
| `[добавляй]` | | |

---

## ДИЗАЙН-СИСТЕМА (если есть)

```
Основной цвет:  [#HEX]
Акцент:         [#HEX]
Фон:            [#HEX]
Текст:          [#HEX]
Граница:        [#HEX]
Border-radius:  [px] (карточки), [px] (кнопки)
```

Breakpoints:
- `sm`: 640px
- `md`: 768px
- `lg`: 1024px
- `xl`: 1280px

---

## ДЕПЛОЙ

```bash
# Production:
[команда деплоя]

# Миграции БД:
[команда миграции]

# Логи:
[команда логов]
```

---

## ВАЖНЫЕ ПАТТЕРНЫ

### [Паттерн 1 — например: Авторизация]
```
[описание как работает + пример кода если нужно]
```

### [Паттерн 2 — например: Обработка ошибок]
```
[описание]
```

---

## ФАЙЛЫ — НЕ ТРОГАТЬ БЕЗ ПРИЧИНЫ

| Файл | Почему осторожно |
|---|---|
| `[путь]` | [причина] |
| `[добавляй]` | |

---

> Этот файл — живой документ. Обновлять после каждой задачи которая добавляет новые endpoint'ы, компоненты или поля БД. При добавлении нового раздела — обновить TOC-таблицу в начале файла.
