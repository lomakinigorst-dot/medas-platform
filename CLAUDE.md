# CLAUDE.md — MEDAS (Медицинская SaaS-платформа)

---

## ENTRY — карта активации

| Ситуация | Что делать |
|---|---|
| Начало любой сессии | Блок 0.1 — сначала, всегда |
| Получил задачи | Блок 0.2 — список задач текстом ДО инструментов |
| Нетривиальная задача | `/planning-with-files:plan` → жди подтверждения |
| Работаю над задачей | Блок 0.3 — выполнение + тест |
| Задача завершена | Блок 0.10 чек-лист → Блок 0.5 (5 шагов) |
| Последняя задача сессии | Блок 0.4 — итоговый отчёт |
| Скопируй / сделай копию | Блок 0.8 — протокол «Копия» |
| Новая страница / дизайн / лендинг | WEBSITE.md — 3 сценария |
| Задача требует UI-дизайн / E2E тест / парсинг | Блок 0.9 — on-demand инструменты |

---

## 4 АБСОЛЮТНЫХ ПРАВИЛА

1. **Список задач** текстом в ответе — ДО первого вызова любого инструмента
2. **Скиллы** — читать `~/.claude/SKILLS.md` через Read, НЕ запускать `claude skills` из Bash
3. **ЛЮБАЯ задача** (даже «простая») → `/planning-with-files:plan` → «Жду подтверждения» → только потом делать. Исключений нет. «Тривиальность» — не критерий.
4. Первый ответ после сводки («continued from...») → начинать с «Выполняю Блок 0.1» → три параллельных Read → только потом текст.

---

## 1. Think Before Coding

**Don't assume. Don't hide confusion. Surface tradeoffs.**

Before implementing:
- State your assumptions explicitly. If uncertain, ask.
- If multiple interpretations exist, present them - don't pick silently.
- If a simpler approach exists, say so. Push back when warranted.
- If something is unclear, stop. Name what's confusing. Ask.

## 2. Simplicity First

**Minimum code that solves the problem. Nothing speculative.**

- No features beyond what was asked.
- No abstractions for single-use code.
- No "flexibility" or "configurability" that wasn't requested.
- No error handling for impossible scenarios.
- If you write 200 lines and it could be 50, rewrite it.

Ask yourself: "Would a senior engineer say this is overcomplicated?" If yes, simplify.

## 3. Surgical Changes

**Touch only what you must. Clean up only your own mess.**

When editing existing code:
- Don't "improve" adjacent code, comments, or formatting.
- Don't refactor things that aren't broken.
- Match existing style, even if you'd do it differently.
- If you notice unrelated dead code, mention it - don't delete it.

When your changes create orphans:
- Remove imports/variables/functions that YOUR changes made unused.
- Don't remove pre-existing dead code unless asked.

The test: Every changed line should trace directly to the user's request.

## 4. Goal-Driven Execution

**Define success criteria. Loop until verified.**

Transform tasks into verifiable goals:
- "Add validation" → "Write tests for invalid inputs, then make them pass"
- "Fix the bug" → "Write a test that reproduces it, then make it pass"
- "Refactor X" → "Ensure tests pass before and after"

For multi-step tasks, state a brief plan:
```
1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]
```

Strong success criteria let you loop independently. Weak criteria ("make it work") require constant clarification.

---

## Блок 0.1 — Старт каждой сессии

⚠️ "Resume directly" из сводки НЕ отменяет этот блок. Выполнить всё ниже до любых других действий.
⚠️ Первое слово первого ответа после любой сводки: «Выполняю Блок 0.1» — иначе СТОП.

1. Написать в тексте: **«Выполняю Блок 0.1»** — до любых инструментов.
2. Прочитать `~/.claude/SKILLS.md` — список всех скиллов (Read, не Bash).
3. CLAUDE.md и MEMORY.md уже в системном контексте — НЕ читать вручную через инструменты
4. Прочитать одним батчем:
   - Всегда: `~/.claude/projects/-Users-igor-Documents-CLAUDE-CODE-Сайт-медас/memory/project_status.md`
   - Если задача касается кода: добавить `CONTEXT.md` + `PLAN.md` + `progress.md` + `CODE_MAP.md`
5. Запустить `/planning-with-files:status` — проверить активный план
6. Активный план → продолжить по нему. Нет → ждать задач.

---

## Блок 0.2 — Получение задач

СТРОГИЙ ПОРЯДОК (нарушать нельзя):

1. Первые строки ответа — ВСЕГДА:
   ```
   ## Задачи:
   1. [первая задача]
   ```
   СТОП-ПРОВЕРКА: если первое слово не «##» — остановиться, переписать с начала.

2. **Всегда** → `/planning-with-files:plan`. Без исключений. Единственное исключение: пользователь явно написал «без плана» / «сразу делай».

3. Зафиксировать все задачи в TodoWrite — после скилла (если запускался), до начала работы.

4. После плана → «Жду подтверждения» → не начинать до ответа пользователя.

5. Добавлена задача в середине → завершить атомарный шаг → зафиксировать progress.md → добавить в TodoWrite → оценить нужен ли /planning-with-files:plan.

---

## Блок 0.3 — Выполнение задач

- Задачи строго по одной, последовательно.
- ✅ только после ручного теста на реальном URL или curl — код в редакторе не считается.
- Изменены .py/.ts/.tsx/.js → ручной тест + ОБЯЗАТЕЛЬНО `/test`. Тип изменения не имеет значения.
- Единственный N/A для /test: «N/A: в задаче нет .py/.ts/.tsx/.js»
- UI → тест на реальном URL. Backend → curl + проверка логов.
- Ошибка → зафиксировать в TodoWrite как блокер, сообщить пользователю.
- Задача выполнена → Блок 0.10 чек-лист → Блок 0.5 (5 шагов).

---

## Блок 0.4 — Итоговый отчёт (после последней задачи сессии)

```
## Итог: [N из M задач выполнено]

### Задача X — [название] ✅/❌/⚠️
Что сделано: [конкретно, 1-2 предложения]
Файлы изменены: [список]
Что проверить: [URL или curl-команда — не «проверить что работает»]
Ошибки: [или явно «нет»]

---
## Рекомендации
N. [Что сделать]: [конкретный риск если не сделать]

## Следующий шаг
[конкретная задача — не «продолжить разработку»]
```

Каждая задача = своя секция. «Не проверено» = явно ⚠️. N/A = написать причину.  
Рекомендации — минимум 3 пункта, нетривиальных, с обоснованием риска.

---

## Блок 0.5 — После каждой задачи (6 шагов)

а) TodoWrite — пометить ✅  
б) progress.md — обновить «Текущий шаг» + секция «БЫСТРОЕ ВОЗОБНОВЛЕНИЕ» (5 строк)  
в) `task_plan.md` — если задача соответствует фазе плана: изменить `Status: pending` → `Status: complete` (или `in_progress` / `blocked`) + добавить строку `Done: [что сделано], коммит: [sha]`  
г) `~/.claude/projects/-Users-igor-Documents-CLAUDE-CODE-Сайт-медас/memory/project_status.md` — что сделано  
д) CODE_MAP.md — обновить если новый endpoint / компонент / страница / поле БД  
е) git add + commit + push origin [ветка]  

ПРАВИЛО БАТЧА: шаги б + г + е — ОДНИМ сообщением с параллельными tool calls.  
Нельзя git commit без одновременного обновления progress.md и memory в том же батче.

БЕЗ ЭТИХ ШЕСТИ ШАГОВ — ЗАДАЧА НЕ DONE.

### MEDAS deploy — критические правила

- **Frontend image:** всегда `medas-frontend:latest`
- **Deploy:** `./deploy.sh` из корня проекта (не `docker compose up` напрямую)
- **Frontend verify:** `grep "уникальная строка из бандла"` — не просто `curl -I 200`
- **Backend changes:** `docker cp` + `docker restart` (не пересоздавать контейнер через force-recreate)
- **SSH:** `ssh -i ~/.ssh/server_key root@85.239.44.14`, app в `/app/medas-platform/`
- **Перед force-recreate контейнера:** `docker inspect <name> --format '{{json .NetworkSettings.Networks}}'` → если сетей >1 — добавить их в docker-compose.yml как external перед пересозданием

---

## Блок 0.6 — Когда какой скилл

| Ситуация | Скилл |
|---|---|
| 3+ задачи или нетривиальная (2+ файлов / 100+ строк / новая фича / неясный баг / архитектура) | `/planning-with-files:plan` |
| Проверить статус плана | `/planning-with-files:status` |
| Завершил задачу (не файл — задачу целиком) | `/test` |
| Изменено 3+ файла или 100+ строк кода | `/simplify` |
| Изменил авторизацию / API endpoint / данные пользователей | `/security-review` |
| Создаёшь новую страницу (HTML, лендинг, не Tilda-копию) | `/web-design` → см. WEBSITE.md |
| Брендбук / дизайн-система | `/design-system` |
| UI с нуля или большой редизайн | `/ui-ux-pro-max:ui-ux-pro-max` |
| Landing page на shadcn | `/shadcnblocks:shadcn-ui` |
| Финансовая модель (P&L / cash flow) | `/stratarts:financial-model-architect` |
| Pitch deck для инвесторов | `/stratarts:investor-pitch-deck-builder` |
| Анализ рынка (TAM/SAM/SOM) | `/stratarts:market-opportunity-analyzer` |
| Конкурентный анализ | `/stratarts:competitive-intelligence` |
| GTM стратегия | `/stratarts:go-to-market-planner` |
| SaaS метрики (CAC/LTV/churn/NRR) | `saas-metrics-coach` (skill) |
| DCF оценка / финансовые коэффициенты | `financial-analyst` (skill) |
| Создание PR | `/review` |
| Повторять задачу по интервалу | `/loop` |
| По расписанию / cron | `/schedule` |
| Убрать AI-стиль из текста | `/humanizer` |
| Генерация видео/изображений | `/runway-api:*` |
| Нужен browser/UI тест (E2E playwright) | `/playwright-on` → перезапуск сессии → задача → `/playwright-off` |

TodoWrite — при 2+ задачах или 1 нетривиальной с 2+ шагами.  
Полный справочник → SKILLS.md

---

## Блок 0.9 — ON-DEMAND ИНСТРУМЕНТЫ

⚠️ Эти инструменты НЕ запускаются автоматически — каждый потребляет RAM и API-лимиты.

| Инструмент | Когда нужен | Включить | Выключить |
|---|---|---|---|
| playwright-mcp | E2E тест / browser-автоматизация | `/playwright-on` → restart | `/playwright-off` |
| magic (21st-dev) | UI-дизайн / React-компоненты | `/mcp-magic-on` → restart | `/mcp-magic-off` |

**Порядок:**
1. Запусти `/playwright-on` или `/mcp-magic-on`
2. Перезапусти VS Code
3. Выполни задачу
4. Запусти `/playwright-off` или `/mcp-magic-off` → перезапусти VS Code

❌ Не держать включёнными после задачи
❌ Не включать если задача явно не требует этого инструмента

---

## Блок 0.7 — Красные линии

**ПРОЦЕСС:**  
❌ Пропускать Блок 0.1 при старте (даже если «контекст уже известен»)  
❌ «resume directly» отменяет Блок 0.1 — НЕТ, не отменяет  
❌ Вызывать инструмент без написанного списка задач  
❌ Начинать без TodoWrite  
❌ ✅ без теста на реальном URL или curl  
❌ Две задачи за один ✅ — одна задача = один ✅  
❌ Пропускать задачи в итоговом отчёте  
❌ Отчёт без секции «Рекомендации»  
❌ Параллельное выполнение задач  
❌ Игнорировать скиллы  
❌ Создавать task_plan.md вместо `/planning-with-files:plan`  
❌ Пропускать `/planning-with-files:plan` при 3+ задачах  
❌ Продолжать если скилл не запустился — стоп, сообщить пользователю  
❌ ✅ если push failed — задача не done до успешного push  
❌ Пропускать /test при изменениях .py/.ts/.tsx/.js  
❌ «N/A: рефакторинг/стили/структура» для /test — тип не освобождает  
❌ 3+ файла или 100+ строк без /simplify  
❌ Auth/API/данные пользователей без /security-review  
❌ git commit без progress.md + memory в том же батче tool calls  
❌ Начинать следующую задачу до написания чек-листа Блока 0.10  
❌ Читать CLAUDE.md или MEMORY.md через инструменты  
❌ Начинать ответ после сводки без «Выполняю Блок 0.1» и параллельных Read  
❌ Ставить N/A на CODE_MAP если добавлен новый .py или .ts/.tsx файл  
❌ Писать блок 0.10 один раз в конце сессии — только после каждой задачи отдельно  
❌ Пропускать план считая задачу «тривиальной» — любая задача требует плана  

**КОД:**  
❌ Глотать ошибки (except: pass, .catch(() => {})) — всегда логировать  
❌ Коммитить секреты, пароли, ключи — только в .env на сервере  
❌ Рефакторинг за рамками задачи  
❌ Удалять функционал без явного разрешения пользователя  

---

## Блок 0.8 — Протокол «Копия»

⚠️ АКТИВИРУЕТСЯ: скопируй / сделай копию / один в один / точная копия / вытащи / выгрузи / перенеси

| Команда | Что делать |
|---|---|
| скопируй / один в один / точная копия / сделай как там | 100%: тексты + изображения + структура + функционал. Только брендинг меняется. |
| вытащи / выгрузи / перенеси | То же что «скопируй» |
| переоформи под нас / оформи в нашем стиле | Сначала полная копия → потом брендинг |
| сделай похожее / вдохновись | НЕ копия — спросить: «Что взять из образца, что придумать?» |
| теперь доработай / добавь идеи | ТОЛЬКО после этих слов — улучшать |

**4 ФАЗЫ (нарушить нельзя):**

**ФАЗА 1 — ЗАФИКСИРОВАТЬ ИСТОЧНИК (до любого кода)**  
URL → curl/WebFetch → findings.md: секции по порядку, тексты дословно, URL изображений, функционал.  
Скриншот/PDF → Read всех файлов → findings.md. Непонятно → спросить, не угадывать.

**ФАЗА 2 — ПОДТВЕРЖДЕНИЕ**  
Показать структуру findings.md пользователю. Противоречие → вопрос → ждать ответа.

**ФАЗА 3 — ТОЧНАЯ КОПИЯ**  
Тексты дословно. Изображения — реальные из источника, не заглушки. Порядок секций — тот же. Функционал — воссоздать аналогично. Ничего не добавлять/убирать/переставлять.

**ФАЗА 4 — БРЕНДИНГ (только после готовой копии)**  
Меняем: логотип, основной цвет, контакты, название.  
НЕ меняем: тексты услуг, числа, структуру, изображения (пока пользователь не прислал свои).

❌ Начинать без fetch/read источника  
❌ Реконструировать «по памяти»  
❌ Менять тексты / пропускать изображения / переставлять блоки  
❌ Угадывать нечёткий текст  
❌ «Улучшения от себя» до команды «доработай»  

---

## Блок 0.10 — Чек-лист завершения задачи (ПУБЛИЧНО в ответе)

⚠️ Писать публично ПОСЛЕ КАЖДОЙ ЗАДАЧИ — не один раз в конце сессии.
⚠️ Если задач N — блок 0.10 повторяется N раз.
⚠️ Добавил новый `.py` или `.ts/.tsx` файл → CODE_MAP.md ВСЕГДА обновить (не N/A).
⚠️ После КАЖДОЙ задачи — первый текст после последнего tool call.  
Нельзя начинать следующую задачу до написания этого чека.

```
✅/❌ TodoWrite — помечен
✅/❌ Тест: [URL или curl] → HTTP [код]  /  N/A: только документация
✅/❌ /test → [запущен / недоступен ⚠️ фиксирую / N/A: в задаче нет .py/.ts/.tsx/.js]
      ⛔ «N/A: рефакторинг» / «N/A: стили» / «N/A: структура» — НЕ допустимы
✅/❌ /simplify → [запущен / N/A: < 3 файлов и < 100 строк кода]
✅/❌ /security-review → [запущен / N/A: не менял auth/API/данные пользователей]
✅/❌ progress.md — обновлён
✅/❌ task_plan.md → [статус фазы обновлён / N/A: задача не из плана]
✅/❌ memory — обновлён
✅/❌ CODE_MAP.md → [обновлён / N/A: нет структурных изменений кода]
✅/❌ git push → [sha коммита] / N/A: только документация
```

❌ ✅ если пункт не выполнен  
❌ Молчать о недоступности скилла — писать явно «/test недоступен ⚠️, фиксирую»  
❌ Пропускать строки — каждая строка обязательна (N/A с причиной считается)  
❌ Чек только в конце сессии — после КАЖДОЙ задачи  
❌ Писать один общий блок 0.10 в конце — только после каждой задачи отдельно

---

## Блок 1.1 — Качество кода

- Писать чистый, читаемый код — через 3 месяца должно быть понятно.
- Следовать конвенциям проекта. Не выходить за рамки задачи.
- DRY: 3 повторения = вынести (обязательно). 2 = оставить.
- Нет N+1 запросов к БД. Обрабатывать ошибки явно: raise/throw с понятным сообщением.
- Никаких `any` — только explicit types или unknown с проверкой типа.
- Комментарий = объяснение ПОЧЕМУ, не ЧТО.

---

## Блок 1.2 — Стек MEDAS

**Frontend:** Next.js 15 (App Router) + Tailwind CSS + shadcn/ui + TypeScript strict  
**Backend:** FastAPI Python 3.12, SQLAlchemy 2.0 async, Pydantic v2, Alembic (миграции)  
**БД:** PostgreSQL 16 + Redis (кэш/сессии) + MinIO (фото врачей, S3-совместимый)  
**Auth:** JWT + refresh tokens (httpOnly cookies)  
**Деплой:** Docker Compose + Nginx, CI/CD GitHub Actions (push в main → автодеплой)  
**VPS:** 85.239.44.14, app в `/app/medas-platform/`, deploy: `./deploy.sh`  
**Домен (dev):** saas.med-as.ru | **API:** api.med-as.ru | **Prod (будущий):** med-as.ru  

**Паттерны Next.js App Router:**
- Публичные страницы (`/search`, `/doctor/[slug]`, `/clinic/[slug]`) — SSR для SEO
- ЛК пациента/клиники/врача — client components + React Query
- Все API вызовы через `src/lib/api.ts`

**Паттерны FastAPI:**
- Роутеры в `backend/app/api/v1/`
- Миграции через Alembic — никаких `ALTER TABLE` вручную
- Async везде: `async def`, `await session.execute()`
- CORS включает ОБА домена: saas.med-as.ru + med-as.ru

---

## Блок 2.1 — Типы тестов

| Тип | Когда | Скилл |
|---|---|---|
| Smoke | После каждого деплоя | `/test` |
| Unit | После изолированной логики | вручную |
| Integration | После изменения API или БД | вручную |
| E2E | После больших фич / редизайна | `/test` |
| Security | После изменений auth/API/данных | `/security-review` |

---

## Блок 2.2 — E2E чеклист MEDAS

После правки кода — проверить затронутые части.  
После деплоя — весь чеклист полностью.

- [ ] Главная `/` — загружается, нет ошибок в консоли браузера
- [ ] Поиск `/search` — список врачей отображается
- [ ] Профиль врача `/doctor/[slug]` — SSR, данные врача загружаются
- [ ] Страница клиники `/clinic/[slug]` — SSR, данные клиники загружаются
- [ ] Авторизация `/login` — вход работает, JWT устанавливается
- [ ] Регистрация `/register` — пациент может создать аккаунт
- [ ] ЛК пациента `/cabinet/patient` — protected route, редирект на /login без auth
- [ ] ЛК клиники `/cabinet/clinic` — protected route работает
- [ ] API health: `curl https://api.med-as.ru/health` → 200
- [ ] Нет новых ошибок в консоли браузера

---

## Блок 3.1 — Definition of Done

- [ ] Код написан и работает (на сервере, не только локально)
- [ ] Протестировано на реальном URL или curl
- [ ] Нет новых ошибок в консоли и логах
- [ ] UI затронут → проверено на 375px (DevTools). Неприменимо → «N/A: [причина]»
- [ ] TodoWrite — ✅
- [ ] progress.md — обновлён «Текущий шаг»
- [ ] project_status.md в памяти проекта (путь → Block 5.1) — записано что сделано
- [ ] CODE_MAP.md — обновлён (или «N/A: нет структурных изменений»)
- [ ] git commit + push

ЕСЛИ ХОТЯ БЫ ОДИН ПУНКТ НЕ ВЫПОЛНЕН — ЗАДАЧА НЕ DONE.

---

## Блок 3.2 — Мобильная адаптация

Каждая новая страница или компонент с изменённой структурой:
- Breakpoint `lg` (1024px) — мобиле vs десктоп
- Тест на 375px (iPhone SE) и 768px (планшет)
- Нет фиксированных px ширин без медиа-запроса

---

## Блок 4.1 — Три системы памяти

| Система | Для чего | Когда читать | Когда обновлять |
|---|---|---|---|
| **MEMORY.md** (файлы) | Правила, профиль, история сессий | Уже в контексте — не читать вручную | После каждой задачи (project_status.md) |
| **CODE_MAP.md** | Карта кода: файлы, endpoints, компоненты | Только нужный раздел | После нового endpoint / компонента / страницы / поля БД |
| **MCP memory граф** | Архитектурные сущности и связи | При исследовании архитектурных связей | При добавлении новых архитектурных единиц |

---

## Блок 4.2 — Когда обновлять CODE_MAP.md

✅ Новый API endpoint → добавить (файл + строка)  
✅ Новый компонент/страница → добавить в маппинг  
✅ Новое поле в БД-модели → обновить схему  
✅ Новый сервис / архитектурный паттерн → обновить структуру  
❌ Стили, тексты, цвета → не трогать  

При добавлении нового раздела → обновить TOC-таблицу в начале CODE_MAP.md с правильным номером строки.

---

## Блок 4.3 — MCP Memory

- `mcp_memory_create_entities` — добавить сущность (компонент, endpoint, сервис)
- `mcp_memory_create_relations` — добавить связь (uses/depends_on/renders)
- `mcp_memory_search_nodes` — найти по запросу
- `mcp_memory_read_graph` — весь граф

Что НЕ хранить: правила работы, статус задач → это в MEMORY.md файлах.

---

## Блок 5.1 — Память проекта MEDAS

**Путь:** `~/.claude/projects/-Users-igor-Documents-CLAUDE-CODE-Сайт-медас/memory/`

Обновляется в Блоке 0.5 шаг в). Никогда не хранить в памяти:
- Код, паттерны, структуру файлов → читать из кода напрямую
- Git-историю → `git log`
- Временные состояния текущей сессии → TodoWrite
